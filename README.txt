- CONTACT MANAGEMENT SYSTEM -

TO EXECUTE: After downloading the folder, enter on it through the CLI and just type "make" on it to compile/execute the program.

The folder contains:
    - HTML/LaTeX Documentation (If you want to see the HTML page with all the documentation, just click on "index", inside the "html" folder)
    - Doxyfile
    - main.c/functions.c/functions.h
    - Makefile
    - LICENSE
    - README